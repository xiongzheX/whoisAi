# 谁是AI 迁移清单

## 1. 迁移目标

将当前项目迁移到：

`/Users/xiongzhe/myIdea/谁是AI`

并保留现有像素风资源、静态界面和 Socket.IO 联机能力。

## 2. 新目录准备

- 确认新目录存在
- 确认 Node 依赖可重新安装
- 确认新项目与旧项目相互独立

## 3. 文件迁移

建议迁移内容：

- `server/`
- `client/`
- `test/`
- `package.json`
- 资源图目录

## 4. 文案替换

需要统一替换为 `谁是AI` 主题：

- 页面标题
- 按钮文案
- 阶段提示
- 消息提示
- 结算文案
- 注释中的旧主题名称

## 5. 资源映射

如果图片文件名仍然保留旧命名，可以先不改文件名，先做映射处理：

- 逻辑层使用新名称
- 文件层暂时保留旧资源名
- 后续再统一重命名

## 6. 代码整理

建议同步处理：

- 去掉重复函数定义
- 梳理常量命名
- 抽离重复逻辑
- 统一 Socket 事件名
- 清理无关测试代码

## 7. 启动验证

迁移后建议按以下顺序检查：

1. 安装依赖
2. 启动服务
3. 打开首页
4. 加入房间
5. 触发开局
6. 跑完整局

## 8. 验收标准

- 新目录下项目能独立启动
- 页面标题和文案统一为 `谁是AI`
- 现有资源图正常加载
- 多人联机可用
- 一局流程可完整跑通
- 没有明显旧主题残留

